# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.80"
name = "rusty_h264-common"
version = "0.9.1"
authors = ["Mata Network"]
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Shared primitives for the rusty_h264 pure-Rust H.264 codec: bitstream I/O, Exp-Golomb, NAL/Annex-B, transforms, intra prediction, motion compensation and deblocking. forbid(unsafe). BSD-2."
homepage = "https://www.mata.network/"
documentation = "https://docs.rs/rusty_h264-common"
readme = "README.md"
keywords = [
    "h264",
    "avc",
    "video",
    "codec",
    "bitstream",
]
categories = [
    "multimedia::video",
    "multimedia::encoding",
    "encoding",
]
license = "BSD-2-Clause"
repository = "https://github.com/remade-with-rust/rusty_h264"

[features]
asm = ["dep:rusty_h264-accel"]
# --- edith patch: `global-alloc` is off by default and inert when asked for ---
# It stays *declared* because `rusty_h264-{decoder,encoder}`'s own defaults name
# `rusty_h264-common/global-alloc`, so removing it would break their manifests;
# it just no longer pulls `rusty_alloc-api` in, and lib.rs no longer installs the
# `#[global_allocator]`. See the note in the workspace root Cargo.toml.
default = []
global-alloc = []
profile = []

[lib]
name = "rusty_h264_common"
path = "src/lib.rs"

[[example]]
name = "bs_layout_ceiling"
path = "examples/bs_layout_ceiling.rs"

[[example]]
name = "deblock_anatomy"
path = "examples/deblock_anatomy.rs"

[dependencies.bytemuck]
version = "1"

[dependencies.rusty_h264-accel]
version = "0.9.1"
optional = true

[dependencies.wide]
version = "0.7"
